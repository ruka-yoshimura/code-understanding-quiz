<!DOCTYPE html>
<html>

<head>
  <title>CodeUnderstandingQuiz</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <% flash.each do |message_type, message| %>
  <div class="flash-message <%= message_type %>" style="padding: 10px; margin-bottom: 10px; border: 1px solid #ccc; background-color: #f0f0f0;">
    <%= message %>
  </div>
  <% end %>
  <%= yield %>
</body>

</html>