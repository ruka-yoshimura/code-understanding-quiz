<div class="max-w-2xl mx-auto py-12 px-4">
  <div class="glass p-8 md:p-10 rounded-3xl shadow-2xl relative">
    <div class="absolute -top-10 left-1/2 -translate-x-1/2 w-20 h-20 bg-primary/20 rounded-2xl blur-2xl"></div>

    <div class="text-center mb-10">
      <h2 class="text-3xl font-black text-white italic tracking-tighter"><%= t('.title', resource: resource.model_name.human) %></h2>
      <p class="text-slate-500 text-sm mt-2">あなたのエンジニアプロフィールを管理します。</p>
    </div>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <% if resource.demo_user? %>
    <div class="mb-8 p-4 bg-amber-500/10 border border-amber-500/20 rounded-xl">
      <p class="text-amber-400 text-sm font-bold m-0 text-center">デモユーザーはプロフィールの変更やアカウントの削除ができません。</p>
    </div>
    <% end %>

    <div class="space-y-6">
      <div>
        <%= f.label :name, "ユーザー名", class: "form-label" %>
        <%= f.text_field :name, autofocus: true, disabled: resource.demo_user?, value: resource.display_name, class: "form-input" %>
      </div>

      <div>
        <%= f.label :email, "メールアドレス", class: "form-label" %>
        <%= f.email_field :email, autocomplete: "email", disabled: resource.demo_user?, class: "form-input" %>
      </div>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div class="p-3 bg-emerald-500/10 border border-emerald-500/20 rounded-lg text-emerald-400 text-sm">
        現在、<%= resource.unconfirmed_email %> の認証を待機しています。
      </div>
      <% end %>

      <div class="pt-4 border-t border-white/5 mt-8">
        <h3 class="text-slate-300 font-bold mb-4 flex items-center gap-2">
          <span>パスワードの変更</span>
          <span class="text-[10px] text-slate-500 font-normal">（変更しない場合は空欄）</span>
        </h3>
        <div class="space-y-4">
          <div>
            <%= f.label :password, "新しいパスワード", class: "form-label" %>
            <%= f.password_field :password, autocomplete: "new-password", disabled: resource.demo_user?, placeholder: "••••••••", class: "form-input" %>
            <% if @minimum_password_length %>
            <p class="text-[10px] text-slate-500 mt-1 uppercase tracking-widest font-black">※<%= @minimum_password_length %>文字以上</p>
            <% end %>
          </div>

          <div>
            <%= f.label :password_confirmation, "新しいパスワード（確認用）", class: "form-label" %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", disabled: resource.demo_user?, placeholder: "••••••••", class: "form-input" %>
          </div>
        </div>
      </div>

      <div class="pt-6 border-t border-white/5 mt-8 bg-white/5 -mx-8 px-8 py-6 rounded-b-3xl">
        <div class="mb-4">
          <%= f.label :current_password, "現在のパスワード", class: "form-label" %>
          <p class="text-xs text-slate-500 mb-2 font-medium tracking-tight italic">※変更を保存するには現在のパスワードが必要です</p>
          <%= f.password_field :current_password, autocomplete: "current-password", disabled: resource.demo_user?, placeholder: "••••••••", class: "form-input" %>
        </div>

        <div class="pt-4">
          <%= f.submit "変更を保存する ✨", class: "btn-primary w-full py-4 text-lg", disabled: resource.demo_user? %>
        </div>
      </div>
    </div>
    <% end %>

    <div class="mt-8 text-center flex flex-col gap-6">
      <% if !resource.demo_user? %>
      <div class="p-6 rounded-2xl bg-rose-500/5 border border-rose-500/10">
        <h3 class="text-rose-400 font-black text-xs uppercase tracking-widest mb-2">危険な操作</h3>
        <p class="text-slate-500 text-[11px] mb-4 leading-relaxed">アカウントを削除すると、これまでの学習ログ、レベル、ランクはすべて失われます。</p>
        <%= button_to "アカウントを完全に削除する", registration_path(resource_name),
                data: { confirm: "本当に退会しますか？この操作は取り消せません。", turbo_confirm: "本当に退会しますか？この操作は取り消せません。" },
                method: :delete,
                class: "text-rose-400 hover:text-rose-300 text-xs font-bold underline transition-colors" %>
      </div>
      <% end %>

      <%= link_to "← ホームに戻る", root_path, class: "text-slate-500 hover:text-slate-300 text-sm transition-colors" %>
    </div>
  </div>
</div>
