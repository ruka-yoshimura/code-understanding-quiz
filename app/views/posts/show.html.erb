<h1><%= @post.title %></h1>

<p>
  <strong>投稿者:</strong>
  <%= @post.user.email %> (レベル: <%= @post.user.level %>)
</p>

<div class="code-block" style="background-color: #f4f4f4; padding: 10px; border-radius: 5px;">
  <pre><code><%= @post.content %></code></pre>
</div>

<br>

<%= button_to "このコードでクイズを作る", quizzes_path(post_id: @post.id), method: :post, class: "button-primary", form: { data: { turbo: false } } %>
<br>
<%= link_to '戻る', root_path %>
